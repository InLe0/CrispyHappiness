<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="CrispyHappiness.View.LoginView">
    <ContentPage.Content>
        <StackLayout>
            <Entry x:Name="userName" Text="{Binding Username}"/>
            <Entry x:Name="password" Text="{Binding Password}"/>
            <ImageButton x:Name="loginButton" HeightRequest="75" WidthRequest="75" HorizontalOptions="Center" VerticalOptions="Center" Source="selfkeylogo.png" Command="{Binding GetInHereCommand}"/>
        </StackLayout>
    
            
        
    </ContentPage.Content>
</ContentPage>